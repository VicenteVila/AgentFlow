[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentflow"
version = "2.0.0"
description = "Parse and visualize AI agent control flows as Excalidraw, SVG, Mermaid, HTML, ASCII, DOT, and sequence diagrams"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{ name = "Vicente Vila", email = "vicentevilaramirez@gmail.com" }]
keywords = ["excalidraw", "flowchart", "ai-agent", "visualization", "diagram", "svg", "sequence-diagram", "mermaid"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Visualization",
]

[project.urls]
Homepage = "https://github.com/VicenteVila/AgentFlow"
Repository = "https://github.com/VicenteVila/AgentFlow"
Issues = "https://github.com/VicenteVila/AgentFlow/issues"

dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0", "ruff>=0.4"]

[project.scripts]
agentflow = "agentflow.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["agentflow"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "SIM", "B", "UP"]
ignore = [
    "E501",  # long lines in diagram labels are intentional
]
